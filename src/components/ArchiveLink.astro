---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<div class="mt-6">
  <a
    href={href}
    class="inline-flex items-center text-primary hover:text-secondary transition-colors duration-300"
  >
    <span class="mr-2">{label}</span>
    <span class="text-lg">→</span>
  </a>
</div>